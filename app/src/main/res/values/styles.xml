<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="alertDialogStyle">@style/AppAlertDialogStyle</item>-->
        <item name="alertDialogTheme">@style/AppAlertDialogTheme</item>
    </style>

    <style name="SearchEtStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="AlertButtonStyle">
        <item name="android:background">@drawable/shape_square_fillet_orange_5</item>
        <item name="android:textColor">#f00</item>
    </style>

    <style name="AppAlertDialogTheme" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@drawable/rounded_corners_bg</item>
        <item name="buttonStyle">@style/AlertButtonStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/AlertButtonStyle</item>
    </style>
    <!--<style name="AppAlertDialogStyle" parent="AlertDialog.AppCompat">-->
        <!--<item name="android:windowBackground">@drawable/rounded_corners_bg</item>-->
        <!--<item name="buttonStyle">@style/AlertButtonStyle</item>-->
    <!--</style>-->

</resources>
