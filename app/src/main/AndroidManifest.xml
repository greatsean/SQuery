<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sean.myquery">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.fang.LunhouSearchActivity"
            android:label="轮候查询"/>
        <activity
            android:name=".ui.shebao.ShebaoLoginActivity"
            android:label="社保登录"/>
        <activity
            android:name=".ui.fang.LunhouDetailActivity"
            android:label="轮候详情"/>
        <activity android:name=".ui.test.Test1Activity">
        </activity>
        <activity android:name=".file.FileListActivity">
        </activity>
    </application>

</manifest>